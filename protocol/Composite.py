from protocol.Component import *
from protocol.Visitor import *

class Composite (Component):

    def load(self):
        pass

    def process(self, v):
        if not elements:
          load()
        for x in elements:
          x.accept(v)




